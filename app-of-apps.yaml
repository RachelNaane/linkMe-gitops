apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-of-apps
  namespace: default
spec:
  project: default
  source:
    repoURL: https://github.com/RachelNaane/notes-app.git
    targetRevision: HEAD 
    path: infra-aplications
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  
  syncPolicy:
    syncOptions:
    - CreateNamespace=true
  
    automated:
      selfHeal: true #if manual changes are applied that are not in source code-argocd will delete them
      prune: true #argocd pulls source code every 3m